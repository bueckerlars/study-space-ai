# OCR Service Configuration
# Port on which the API server will run
OCR_SERVICE_PORT=8000

# Comma-separated list of allowed origins for CORS
# Use * for allowing all origins (not recommended in production)
# Example for specific origins:
ALLOWED_ORIGINS=http://localhost:5066,http://localhost:5066,https://your-production-domain.com