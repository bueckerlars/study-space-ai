export * from './DatabaseController';

// Export the singleton instances by default
import { databaseController } from './DatabaseController';

export {
  databaseController
};

export default {
  databaseController
};
